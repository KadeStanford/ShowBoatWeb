rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Profile photos: owner writes, public reads (download URLs are token-protected anyway)
    match /profilePhotos/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
